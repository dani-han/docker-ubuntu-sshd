version: "3.9"
services: 
    sshd:
        build:
            context: .
            dockerfile: sshd.Dockerfile
            args: 
                TAG: latest
        environment: 
            HOSTHOME: $HOME
        ports: 
            - "22:22"
        volumes:
            - ~/:/host/$HOME
